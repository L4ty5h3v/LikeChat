# 🚀 Быстрый старт: Настройка покупки токена

## 📋 Что нужно сделать:

### 1. Создайте файл `.env.local`

Создайте файл `.env.local` в корне проекта со следующим содержимым:

```env
# Адрес токена (уже настроен)
NEXT_PUBLIC_TOKEN_CONTRACT_ADDRESS=0x454b4180bc715ba6a8568a16f1f9a4b114a329a6

# ⚠️ ВАЖНО: Установите адрес вашего контракта продажи
# Выберите ОДИН вариант:

# ВАРИАНТ A: Покупка через ETH
NEXT_PUBLIC_TOKEN_SALE_CONTRACT_ADDRESS=0xВАШ_АДРЕС_КОНТРАКТА
NEXT_PUBLIC_USE_USDC_FOR_PURCHASE=false

# ВАРИАНТ B: Покупка через USDC (раскомментируйте, если используете)
# NEXT_PUBLIC_TOKEN_SALE_USDC_CONTRACT_ADDRESS=0xВАШ_АДРЕС_КОНТРАКТА
# NEXT_PUBLIC_USE_USDC_FOR_PURCHASE=true
# NEXT_PUBLIC_USDC_CONTRACT_ADDRESS=0x833589fCD6eDb6E08f4c7C32D4f71b54bdA02913

# Опционально (для работы базы данных)
# UPSTASH_REDIS_REST_URL=your-url
# UPSTASH_REDIS_REST_TOKEN=your-token
# NEYNAR_API_KEY=your-key

NEXT_PUBLIC_APP_URL=http://localhost:3000
```

### 2. Разверните контракт продажи (если еще не развернут)

**Если у вас НЕТ контракта:**

1. Используйте контракт из `contracts/MrsCryptoTokenSale.sol` (для ETH)
   или `contracts/MrsCryptoTokenSaleUSDC.sol` (для USDC)

2. Разверните на Base:
   - Используйте Hardhat, Remix или другой инструмент
   - См. подробную инструкцию в `SETUP_CONTRACT.md`

3. После развертывания:
   - Скопируйте адрес контракта
   - Вставьте в `.env.local`
   - Переведите токены на контракт продажи

**Если у вас УЖЕ ЕСТЬ контракт:**

1. Просто скопируйте адрес контракта
2. Вставьте в `.env.local` в переменную `NEXT_PUBLIC_TOKEN_SALE_CONTRACT_ADDRESS`

### 3. Проверьте конфигурацию

```bash
npm run check:config
```

Должно показать: ✅ Конфигурация в порядке!

### 4. Запустите приложение

```bash
npm run dev
```

Откройте http://localhost:3000

### 5. Протестируйте покупку

1. Перейдите на `/buyToken`
2. Подключите кошелек (Farcaster или MetaMask)
3. Убедитесь, что в кошельке есть:
   - **Для ETH**: ETH на Base (для покупки + gas)
   - **Для USDC**: USDC на Base + ETH для gas
4. Нажмите "Buy Mrs Crypto Token"
5. Подтвердите транзакцию

---

## ✅ Чек-лист готовности:

- [ ] Файл `.env.local` создан
- [ ] Адрес контракта продажи установлен
- [ ] Контракт развернут на Base
- [ ] Контракт пополнен токенами
- [ ] `npm run check:config` показывает успех
- [ ] Приложение запущено (`npm run dev`)
- [ ] Кошелек подключен к Base
- [ ] В кошельке есть средства

---

## 🆘 Если что-то не работает:

### Ошибка: "Адрес смарт-контракта продажи не настроен"
- ✅ Решение: Проверьте, что `NEXT_PUBLIC_TOKEN_SALE_CONTRACT_ADDRESS` установлен в `.env.local`
- ✅ Перезапустите сервер после изменения `.env.local`

### Ошибка: "Farcaster Wallet не найден"
- ✅ Решение: Откройте приложение в Farcaster Mini App (через Warpcast)
- ✅ Или используйте MetaMask для тестирования

### Ошибка: "Недостаточно средств"
- ✅ Решение: Пополните кошелек ETH/USDC на Base
- ✅ Убедитесь, что есть ETH для gas fees

### Ошибка: "Insufficient inventory"
- ✅ Решение: Пополните контракт продажи токенами
- ✅ Вызовите `availableTokens()` на контракте для проверки

---

## 📚 Дополнительная документация:

- `SETUP_CONTRACT.md` - Подробная инструкция по развертыванию контракта
- `TESTING_TOKEN_PURCHASE.md` - Полное руководство по тестированию
- `QUICK_TEST.md` - Быстрая инструкция по тестированию
- `TEST_RESULTS.md` - Результаты автоматизированного тестирования

---

## 🎯 Следующие шаги:

1. ✅ Настройте `.env.local` с адресом контракта
2. ✅ Запустите `npm run check:config` для проверки
3. ✅ Запустите `npm run dev`
4. ✅ Протестируйте покупку в браузере

**Готово к тестированию!** 🚀


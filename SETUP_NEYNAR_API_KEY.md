# 🔑 Настройка NEYNAR_API_KEY

## 📋 Шаг 1: Получить Neynar API ключ

1. Откройте https://neynar.com
2. Войдите или зарегистрируйтесь
3. Перейдите в раздел **API Keys** / **Developer Settings**
4. Создайте новый API ключ или скопируйте существующий

## 📝 Шаг 2: Добавить ключ в локальный проект

Откройте файл `.env.local` в корне проекта и добавьте (или обновите) строку:

```env
# Neynar API (серверная переменная - БЕЗ NEXT_PUBLIC_)
NEYNAR_API_KEY=ваш_ключ_здесь
```

**Важно:**
- Используйте `NEYNAR_API_KEY` (БЕЗ `NEXT_PUBLIC_`)
- Это серверная переменная, она не будет доступна в браузере (безопаснее)
- Замените `ваш_ключ_здесь` на реальный ключ из Neynar

## 🚀 Шаг 3: Добавить ключ в Vercel (для продакшена)

1. Откройте https://vercel.com/dashboard
2. Выберите ваш проект `likechat-farcaster`
3. Перейдите в **Settings** → **Environment Variables**
4. Добавьте новую переменную:
   - **Name:** `NEYNAR_API_KEY`
   - **Value:** ваш ключ из Neynar
   - **Environment:** Production, Preview, Development (отметьте все)
5. Нажмите **Save**
6. Перезапустите деплой (или он перезапустится автоматически)

## ✅ Проверка

После добавления ключа:

1. **Локально:** Перезапустите dev сервер:
   ```bash
   npm run dev
   ```

2. **В продакшене:** Проверьте логи Vercel - не должно быть предупреждений `NEYNAR_API_KEY not configured`

## 🔍 Где используется

`NEYNAR_API_KEY` используется в:
- `lib/neynar.ts` - для разрешения коротких ссылок и проверки активности
- `pages/api/verify-activity.ts` - для верификации активности пользователей
- `pages/api/farcaster-user.ts` - для получения данных пользователей

## ⚠️ Безопасность

- **НЕ** коммитьте `.env.local` в Git (он уже в `.gitignore`)
- **НЕ** используйте `NEXT_PUBLIC_NEYNAR_API_KEY` - это сделает ключ доступным в браузере
- Используйте серверную переменную `NEYNAR_API_KEY` для безопасности




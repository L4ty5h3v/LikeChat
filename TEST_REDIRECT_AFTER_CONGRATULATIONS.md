# Тест редиректа после закрытия поздравления

## Текущее поведение

### После публикации ссылки:
1. Пользователь публикует ссылку на странице `/submit`
2. После успешной публикации показывается полноэкранная страница поздравления (`showSuccessModal = true`)
3. Пользователь видит поздравление с кнопкой "Close"

### При нажатии на кнопку "Close":
- **Текущий редирект**: `/` (главная страница) ✅
- **Код**: `router.replace('/')` в `pages/submit.tsx` строка 903

### Дополнительная логика:
- В `pages/_app.tsx` есть обработчик `NotificationRedirectHandler`, который также перенаправляет на главную страницу при возврате в приложение после закрытия уведомления
- Это не должно конфликтовать, так как прямой редирект через `router.replace('/')` имеет приоритет

## Тестирование

### Шаги для тестирования:
1. Запустить приложение
2. Авторизоваться
3. Выбрать активность
4. Выполнить все задачи
5. Опубликовать ссылку
6. Дождаться появления поздравления
7. Нажать кнопку "Close"
8. **Ожидаемый результат**: Редирект на главную страницу `/`

### Проверка в консоли:
При нажатии на "Close" должно появиться:
```
🔍 [SUBMIT] Button "Close" clicked - redirecting to / (home page)
```

### Проверка URL:
После нажатия "Close" URL должен измениться на `/`

## Изменения

### Изменено:
- `pages/submit.tsx` строка 903: изменен редирект с `/tasks` на `/`

### Добавлено:
- `pages/_app.tsx`: компонент `NotificationRedirectHandler` для обработки возврата в приложение после закрытия уведомлений


# 🔑 Как получить VERCEL_TOKEN

## 📋 Инструкция

### Шаг 1: Откройте страницу токенов Vercel

1. Перейдите по ссылке: **https://vercel.com/account/tokens**
2. Или:
   - Откройте https://vercel.com/dashboard
   - Нажмите на ваш аватар (правый верхний угол)
   - Выберите **Settings**
   - В левом меню выберите **Tokens**

### Шаг 2: Создайте новый токен

1. На странице токенов нажмите кнопку **Create Token**
2. Введите название токена (например: `likechat-farcaster-api`)
3. Выберите срок действия:
   - **No Expiration** (без срока действия) - для постоянного использования
   - Или выберите конкретную дату
4. Нажмите **Create Token**

### Шаг 3: Скопируйте токен

⚠️ **ВАЖНО:** Токен показывается только один раз! Скопируйте его сразу.

1. После создания токена он будет отображен на экране
2. Скопируйте токен полностью (он выглядит как длинная строка символов)
3. Сохраните его в безопасном месте

### Шаг 4: Добавьте токен в проект

1. Создайте или откройте файл `.env.local` в корне проекта
2. Добавьте строку:
   ```
   VERCEL_TOKEN=ваш_токен_здесь
   ```
3. Сохраните файл

### Шаг 5: Используйте скрипт

После добавления токена вы можете использовать скрипты:

```bash
# Получить информацию о проекте (включая описание)
node scripts/get-vercel-project-info.js

# Обновить NEYNAR_API_KEY на Vercel
node scripts/update-neynar-key-vercel.js

# Обновить другие переменные окружения
node scripts/update-vercel-env-api.js
```

## 🔒 Безопасность

- **НЕ коммитьте** файл `.env.local` в Git (он уже в `.gitignore`)
- **НЕ делитесь** токеном публично
- Если токен скомпрометирован, удалите его в Vercel и создайте новый

## 📝 Пример файла .env.local

```env
# Vercel API Token
VERCEL_TOKEN=vercel_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Neynar API Key
NEYNAR_API_KEY=1F4EE142-7FBF-4BBB-83B3-9AF9E1588383

# Другие переменные...
```

## ✅ Проверка

После добавления токена проверьте, что он работает:

```bash
node scripts/get-vercel-project-info.js
```

Если токен правильный, вы увидите информацию о проекте. Если нет - получите ошибку авторизации.







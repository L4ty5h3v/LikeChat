# 🚀 Развертывание контракта продажи для токена MCT

Токен MCT: `0x04d388da70c32fc5876981097c536c51c8d3d236`

## 📋 Быстрый способ: Remix IDE

### Шаг 1: Откройте Remix
https://remix.ethereum.org

### Шаг 2: Создайте файл контракта
1. Создайте файл `MrsCryptoTokenSale.sol`
2. Скопируйте код из `contracts/MrsCryptoTokenSale.sol`

### Шаг 3: Установите OpenZeppelin
Создайте структуру:
- `@openzeppelin/contracts/token/ERC20/IERC20.sol`
- `@openzeppelin/contracts/access/Ownable.sol`

(См. полную инструкцию в `DEPLOY_REMIX.md`)

### Шаг 4: Скомпилируйте
- Версия компилятора: **0.8.20**
- Нажмите "Compile"

### Шаг 5: Разверните
1. Подключите MetaMask к сети **Base**
2. В разделе "Deploy" введите параметры:

**token_ (address):**
```
0x04d388da70c32fc5876981097c536c51c8d3d236
```

**pricePerTokenWei (uint256):**
```
1000000000000000
```
(Это 0.001 ETH за 1 токен, или 0.0001 ETH за 0.10 MCT)

3. Нажмите "Deploy"
4. Подтвердите транзакцию

### Шаг 6: Сохраните адрес контракта

После развертывания скопируйте адрес контракта и добавьте в `.env.local`:

```env
NEXT_PUBLIC_TOKEN_CONTRACT_ADDRESS=0x04d388da70c32fc5876981097c536c51c8d3d236
NEXT_PUBLIC_TOKEN_SALE_CONTRACT_ADDRESS=0xВашАдресКонтрактаПродажи
NEXT_PUBLIC_USE_USDC_FOR_PURCHASE=false
```

### Шаг 7: Пополните контракт токенами MCT

1. Откройте ваш кошелек
2. Найдите токен MCT: `0x04d388da70c32fc5876981097c536c51c8d3d236`
3. Переведите токены на адрес контракта продажи
4. Минимум для тестирования: 10-100 MCT

### Шаг 8: Проверьте

1. Откройте контракт на BaseScan:
   ```
   https://basescan.org/address/YOUR_CONTRACT_ADDRESS
   ```

2. Проверьте цену:
   - Вызовите `pricePerToken()` - должно быть `1000000000000000` (0.001 ETH)

3. Проверьте стоимость 0.10 MCT:
   - Вызовите `costFor(100000000000000000)` - должно быть `100000000000000` (0.0001 ETH)

4. Проверьте баланс токенов:
   - Вызовите `availableTokens()` - должно быть > 0

---

## ✅ Готово к тестированию!

После настройки:

1. Проверьте конфигурацию:
   ```bash
   npm run check:config
   ```

2. Запустите приложение:
   ```bash
   npm run dev
   ```

3. Протестируйте покупку:
   - Откройте `/buyToken`
   - Подключите кошелек
   - Купите 0.10 MCT

---

## 💡 Настройка цены

Если хотите изменить цену после развертывания:

1. Откройте контракт на BaseScan
2. Вызовите функцию `setPricePerToken(uint256 newPrice)`
3. Введите новую цену в wei:
   - 0.001 ETH = `1000000000000000`
   - 0.0005 ETH = `500000000000000`
   - 0.002 ETH = `2000000000000000`

**Примечание:** Только владелец контракта может изменить цену.

---

## 🔄 Для USDC

Если хотите использовать USDC для покупки:

1. Разверните `MrsCryptoTokenSaleUSDC.sol`
2. Параметры:
   - `token_`: `0x04d388da70c32fc5876981097c536c51c8d3d236`
   - `paymentToken_`: `0x833589fCD6eDb6E08f4c7C32D4f71b54bdA02913` (USDC)
   - `pricePerTokenUSDC`: `2500000` (2.5 USDC в 6 decimals)

3. Добавьте в `.env.local`:
```env
NEXT_PUBLIC_TOKEN_SALE_USDC_CONTRACT_ADDRESS=0xВашАдрес
NEXT_PUBLIC_USE_USDC_FOR_PURCHASE=true
```


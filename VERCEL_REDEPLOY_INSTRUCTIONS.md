# 🚀 Инструкция по принудительному Redeploy без кеша

## ⚠️ КРИТИЧЕСКИ ВАЖНО: Модальное окно все еще видно из-за кеша Vercel

Код модального окна **уже удален** из всех файлов. Если оно все еще видно, это **100% кеш Vercel**.

## Способ 1: Через Vercel Dashboard (РЕКОМЕНДУЕТСЯ)

### Шаг 1: Очистите Build Cache

1. Откройте https://vercel.com/dashboard
2. Выберите проект **`likechat-farcaster`**
3. Перейдите в **Settings** → **Git**
4. Найдите секцию **Build Cache**
5. Нажмите **"Clear Build Cache"** или **"Purge Build Cache"**
6. Подтвердите очистку

### Шаг 2: Redeploy БЕЗ кеша

1. В Vercel Dashboard откройте ваш проект
2. Перейдите на вкладку **Deployments**
3. Найдите последний deployment (должен быть коммит `a9f08b2`)
4. Нажмите на **три точки (⋯)** рядом с deployment
5. Выберите **"Redeploy"**
6. **⚠️ КРИТИЧЕСКИ ВАЖНО**: Найдите опцию **"Use existing Build Cache"** → **ОТКЛЮЧИТЕ ЕЕ** (чтобы игнорировать кеш)
   - Если опции нет в меню, нажмите **"Redeploy"** и в появившемся окне отключите кеш
7. Нажмите **"Redeploy"**

### Шаг 3: Дождитесь завершения deployment

- Deployment займет 2-5 минут
- Следите за статусом в Vercel Dashboard
- Дождитесь статуса **"Ready"**

## Способ 2: Через Vercel CLI (Альтернатива)

Если у вас установлен Vercel CLI:

```bash
# 1. Установите Vercel CLI (если еще не установлен)
npm install -g vercel

# 2. Войдите в Vercel
vercel login

# 3. Очистите кеш и сделайте redeploy
vercel env pull --force
vercel --prod --force
```

Или используйте скрипт:
```bash
# Windows PowerShell
.\scripts\force-redeploy.ps1

# Linux/Mac
chmod +x scripts/force-redeploy.sh
./scripts/force-redeploy.sh
```

## Способ 3: Добавьте Alchemy RPC (Опционально, но рекомендуется)

Для улучшения стабильности Uniswap Quote API:

1. Откройте https://vercel.com/dashboard
2. Выберите проект **`likechat-farcaster`**
3. Перейдите в **Settings** → **Environment Variables**
4. Нажмите **"Add New"**
5. Добавьте:
   - **Key**: `ALCHEMY_BASE_RPC_URL`
   - **Value**: `https://base-mainnet.g.alchemy.com/v2/YOUR_API_KEY`
   - **Environment**: Production, Preview, Development (выберите все)
6. Нажмите **"Save"**
7. Сделайте **Redeploy** (см. Способ 1, Шаг 2)

## ✅ Проверка после Redeploy

После завершения deployment:

1. **Очистите кеш браузера**:
   - Откройте DevTools (F12)
   - Application → Storage → Clear site data
   - Или используйте Ctrl+Shift+Delete

2. **Откройте сайт в Incognito режиме** (Ctrl+Shift+N)

3. **Откройте консоль** (F12 → Console)

4. **Проверьте логи**:
   - Должен появиться: `🔍🔍🔍 [_DOCUMENT] Modal removal script loaded - version: ... - IF YOU SEE THIS, SCRIPT IS LOADING! 🔍🔍🔍`
   - Если этого лога **НЕТ** → все еще загружается старый JS из кеша

5. **Проверьте Network tab**:
   - Откройте DevTools → Network
   - Обновите страницу (Ctrl+R)
   - Проверьте все `.js` файлы:
     - **Статус 200** = свежий файл ✅
     - **Статус 304** = старый кеш ❌ (проблема!)

6. **Проверьте модальное окно**:
   - Модальное окно "SYSTEM INITIALIZATION" должно **исчезнуть**
   - Если все еще видно → пришлите логи из консоли

## 🔍 Диагностика

Если модальное окно все еще видно после redeploy:

1. **Проверьте, что deployment действительно обновился**:
   - В Vercel Dashboard → Deployments
   - Последний deployment должен иметь коммит `a9f08b2` или новее
   - Build ID должен быть timestamp (например, `1737123456789`)

2. **Проверьте консоль браузера**:
   - Если нет логов `🔍🔍🔍 [_DOCUMENT] Modal removal script loaded` → загружается старый bundle
   - Если логи есть, но модальное окно видно → пришлите полные логи

3. **Проверьте Network tab**:
   - Все `.js` файлы должны быть со статусом **200**, не **304**
   - Если видите **304** → кеш браузера или CDN

## 📞 Если проблема сохраняется

Если после всех шагов модальное окно все еще видно:

1. Сделайте скриншот консоли браузера (F12 → Console)
2. Сделайте скриншот Network tab (покажите статусы `.js` файлов)
3. Проверьте, какой deployment активен в Vercel Dashboard
4. Пришлите эту информацию для дальнейшей диагностики


# 🔧 Исправление проблемы с NEYNAR_API_KEY на Vercel

## ❌ Проблема
Ошибка "Не удалось получить hash из ссылки" на production (Vercel)

## ✅ Решение

### 1. Настройка API ключа на Vercel

1. Откройте: https://vercel.com/dashboard
2. Выберите проект: **likechat-farcaster**
3. Перейдите в **Settings** → **Environment Variables**
4. Добавьте переменную:
   - **Name**: `NEYNAR_API_KEY`
   - **Value**: `1F4EE142-7FBF-4BBB-83B3-9AF9E1588383`
   - **Environment**: Отметьте все три:
     - ✅ Production
     - ✅ Preview  
     - ✅ Development
5. Нажмите **Save**

### 2. Перезапуск деплоя

**ВАЖНО:** После добавления переменной окружения **ОБЯЗАТЕЛЬНО** перезапустите деплой!

1. В Vercel Dashboard откройте вкладку **Deployments**
2. Найдите последний деплой
3. Нажмите на три точки (⋮) → **Redeploy**
4. Выберите **Use existing Build Cache** (опционально)
5. Нажмите **Redeploy**

### 3. Проверка логов

После перезапуска проверьте логи:

1. Откройте последний деплой
2. Перейдите на вкладку **Functions** → выберите функцию `/api/verify-activity`
3. Проверьте логи на наличие:
   - ✅ `[neynar] resolveCastUrl: successfully resolved` - все работает
   - ❌ `NEYNAR_API_KEY not configured` - ключ не настроен
   - ❌ `API error 401` - неверный ключ
   - ❌ `API error 402` - нужен платный план

## 🔍 Диагностика

Если проблема сохраняется:

1. **Проверьте переменную окружения:**
   - Убедитесь, что `NEYNAR_API_KEY` добавлен для всех окружений
   - Проверьте, что значение правильное (без пробелов, переносов строк)

2. **Проверьте логи Vercel:**
   - Откройте логи последнего деплоя
   - Найдите сообщения с префиксом `[neynar]` или `[VERIFY]`
   - Это покажет точную причину ошибки

3. **Проверьте API ключ:**
   - Убедитесь, что ключ активен на Neynar
   - Проверьте, что платный план активирован

## 📝 Автоматическая настройка (опционально)

Если у вас есть VERCEL_TOKEN, можно использовать скрипт:

```bash
# 1. Получите VERCEL_TOKEN: https://vercel.com/account/tokens
# 2. Добавьте в .env.local: VERCEL_TOKEN=your_token
# 3. Запустите:
node scripts/update-neynar-key-vercel.js
```

## ✅ После исправления

После настройки API ключа и перезапуска деплоя:
- Короткие ссылки `farcaster.xyz/username/0x...` будут разрешаться
- Ошибка "Не удалось получить hash из ссылки" исчезнет
- Все функции приложения будут работать корректно


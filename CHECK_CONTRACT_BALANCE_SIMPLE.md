# 💰 Проверка баланса контракта

## Адрес контракта:
**`0xcd7092246c5DB86bC65C98fD943A18d409fCf03D`** на Base (8453)

## Способ 1: Через приложение (самый простой)

В вашем приложении уже есть компонент `ContractStats`, который показывает баланс. Проверьте, отображается ли он на странице.

Если нет, добавьте его в `App.tsx`:

```tsx
import { ContractStats } from "./ContractStats";

// В компоненте App:
<ContractStats />
```

## Способ 2: Через консоль браузера на странице приложения

Откройте консоль (F12) и выполните:

```javascript
// Если используется wagmi
const { createPublicClient, http, formatUnits } = await import('https://cdn.jsdelivr.net/npm/viem@latest/dist/esm/index.js');

const publicClient = createPublicClient({
  chain: {
    id: 8453,
    name: 'Base',
    network: 'base',
    nativeCurrency: { name: 'Ether', symbol: 'ETH', decimals: 18 },
    rpcUrls: {
      default: { http: ['https://mainnet.base.org'] }
    }
  },
  transport: http('https://mainnet.base.org')
});

const balance = await publicClient.readContract({
  address: '0xcd7092246c5DB86bC65C98fD943A18d409fCf03D',
  abi: [{
    inputs: [],
    name: 'getContractBalance',
    outputs: [{ internalType: 'uint256', name: '', type: 'uint256' }],
    stateMutability: 'view',
    type: 'function'
  }],
  functionName: 'getContractBalance'
});

console.log('Баланс контракта:', formatUnits(balance, 18), 'MCT');
```

## Способ 3: Через Basescan (проверка токенов)

1. Откройте: https://basescan.org/address/0xcd7092246c5DB86bC65C98fD943A18d409fCf03D
2. Перейдите на вкладку **"Token Transfers (ERC-20)"**
3. Найдите транзакции с токеном MCT
4. Проверьте баланс токенов на контракте

**Важно:** Нужно знать адрес токена MCT на Base, чтобы проверить баланс напрямую.

## Способ 4: Через API Basescan

```bash
# Получить список токенов контракта
curl "https://api.basescan.org/api?module=account&action=tokenlist&address=0xcd7092246c5DB86bC65C98fD943A18d409fCf03D&apikey=YourApiKeyToken"
```

## Что проверить:

1. **Баланс MCT токенов** - должно быть достаточно для выплат (минимум 20,000 MCT на каждый клейм)
2. **Размер награды** - 20,000 MCT (проверьте через `dailyRewardAmount`)
3. **Сколько клеймов можно сделать** - баланс / 20,000

## Если баланс низкий:

1. Нужно пополнить контракт MCT токенами
2. Отправьте MCT токены на адрес контракта: `0xcd7092246c5DB86bC65C98fD943A18d409fCf03D`
3. Убедитесь, что отправляете именно MCT токены на сети Base, а не ETH

## Адрес токена MCT:

Нужно найти адрес токена MCT на Base. Обычно он указан в:
- Конфигурации проекта
- Контракте `FarcasterDailyRewards`
- Переменных окружения




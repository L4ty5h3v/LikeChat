# 📋 Объяснение Exec транзакции Farcaster

## Что вы показали:

Это **multicall транзакция** от Farcaster кошелька с функцией:
```
exec(address,address,uint256,address,bytes)
```

---

## 🔍 Структура транзакции:

```json
{
  "atomicRequired": false,
  "calls": [
    {
      "data": "0x...", // Закодированные данные для выполнения
      "to": "0x0000000000001ff3684f28c67538d4d072c22734" // Роутер/Агрегатор
    }
  ],
  "chainId": "0x2105", // Base (8453)
  "from": "0x12029632C95dE52D403e18A03D29648b877b2889",
  "version": "2.0.0"
}
```

---

## 📊 Что происходит внутри:

1. **Роутер:** `0x0000000000001ff3684f28c67538d4d072c22734`
   - Это адрес роутера/агрегатора, который выполняет несколько операций

2. **Внутри данных (data):**
   - Вызовы `approve` для USDC (`0x833589fCD6eDb6E08f4c7C32D4f71b54bdA02913`)
   - Вызовы `transfer` для токенов
   - Swap операции через DEX
   - Различные адреса получателей

3. **Это multicall:**
   - Несколько операций в одной транзакции
   - Все выполняется атомарно (или не атомарно, если `atomicRequired: false`)

---

## 🚀 Как это работает в Farcaster:

1. **Пользователь инициирует swap** через интерфейс
2. **Farcaster кошелек** формирует multicall транзакцию
3. **Роутер** выполняет все операции:
   - Approve токенов (если нужно)
   - Swap через DEX
   - Transfer токенов получателю

---

## ✅ Текущая реализация:

В коде используется **`sdk.actions.openUrl()`** для открытия swap интерфейса:

1. Приложение открывает Uniswap с параметрами swap
2. Пользователь видит детали транзакции
3. Farcaster кошелек автоматически формирует exec транзакцию
4. Пользователь подтверждает
5. Транзакция выполняется

---

## 💡 Почему используется openUrl:

Farcaster SDK **не имеет прямого метода exec** для формирования таких транзакций.

Вместо этого:
- ✅ Используем `openUrl` для открытия swap интерфейса
- ✅ Кошелек сам формирует exec транзакцию
- ✅ Пользователь подтверждает в знакомом интерфейсе

---

## 🔧 Если нужен прямой exec:

Для прямого выполнения exec транзакции нужно:

1. **Сформировать данные транзакции** вручную
2. **Использовать провайдер** для отправки:
   ```typescript
   const signer = await provider.getSigner();
   const tx = await signer.sendTransaction({
     to: routerAddress,
     data: encodedData,
     value: amount,
   });
   ```

Но это сложнее и требует:
- Знания адреса роутера
- Формирования encoded данных
- Обработки всех операций вручную

---

## ✅ Рекомендация:

**Текущий подход правильный:**
- Используем `openUrl` для открытия swap интерфейса
- Кошелек сам формирует exec транзакцию
- Пользователь видит все детали перед подтверждением
- Проще и безопаснее для пользователя

---

## 📝 Итог:

Транзакция exec, которую вы показали - это **результат работы swap интерфейса** Farcaster кошелька. 

Текущая реализация через `openUrl` правильная и будет автоматически генерировать такие exec транзакции при использовании swap.


# Диагностика проблемы с предзаполнением суммы 0.10 USDC

## 🔍 Что я проверил:

### 1. **Официальные типы OnchainKit:**
- `node_modules/@coinbase/onchainkit/dist/minikit/hooks/useSwapToken.d.ts`
- `sellAmount` должен быть в wei формате: `"1000000"` для 1 USDC
- Для 0.10 USDC: `"100000"` (0.10 * 10^6)

### 2. **Тесты OnchainKit:**
- `node_modules/@coinbase/onchainkit/src/minikit/hooks/useSwapToken.test.tsx`
- Все тесты используют wei формат: `sellAmount: '1000000'` для 1 USDC
- Тест для 1.5 USDC: `sellAmount: '1500000'`

### 3. **Типы Farcaster SDK:**
- `node_modules/@coinbase/onchainkit/node_modules/@farcaster/miniapp-core/src/actions/SwapToken.ts`
- Комментарий: "Sell token amount, as numeric string. For example, 1 USDC: 1000000"
- **ВАЖНО:** В комментарии написано "Opens the swap form with the tokens pre-selected" - токены предвыбираются, но НЕ упоминается предзаполнение суммы!

## ⚠️ Возможные причины проблемы:

### 1. **Farcaster SDK может не поддерживать предзаполнение суммы**
- Комментарий говорит только о "tokens pre-selected", не о сумме
- Возможно, `sellAmount` используется только для валидации, а не для предзаполнения UI

### 2. **Формат может быть неправильным**
- Мы пробовали: wei `"100000"` и форматированную строку `"0.1"`
- Согласно документации, должен быть wei формат

### 3. **Farcaster wallet может игнорировать параметры**
- Возможно, wallet открывает форму, но не применяет параметры к UI

## ✅ Что сделано:

1. **Исправлен формат на wei:** `"100000"` для 0.10 USDC
2. **Добавлено детальное логирование:**
   - Что передается в `swapTokenAsync`
   - Что возвращает функция
   - Типы и форматы всех параметров

## 🔬 Как протестировать:

1. Откройте консоль браузера (F12)
2. Нажмите кнопку "BUY TOKEN"
3. Проверьте логи:
   - `🚀 [SWAP] Calling swapTokenAsync NOW with exact params:` - что передается
   - `📤 [SWAP] Parameters being sent to sdk.actions.swapToken:` - детали параметров
   - `📥 [SWAP] swapTokenAsync returned:` - что вернулось

## 📋 Что проверить в логах:

1. **Правильно ли передается sellAmount?**
   - Должно быть: `"100000"`
   - Тип: `string`
   - Длина: `6`

2. **Что возвращает swapTokenAsync?**
   - Если `undefined` или `null` - форма открылась, но параметры могут не применяться
   - Если объект с `success: true` - swap может быть выполнен автоматически

3. **Открывается ли форма вообще?**
   - Если форма не открывается - проблема в другом месте
   - Если открывается с "0" - Farcaster не применяет параметры

## 🎯 Следующие шаги:

1. **Если форма открывается с "0":**
   - Farcaster SDK не поддерживает предзаполнение суммы
   - Нужно использовать компонент `<Swap>` от OnchainKit вместо хука
   - Или показать пользователю подсказку ввести "0.10 USDC" вручную

2. **Если форма не открывается:**
   - Проверить ошибки в консоли
   - Проверить подключение кошелька
   - Проверить сеть (должна быть Base)

3. **Если swap выполняется автоматически:**
   - Это нормально! Параметры работают, просто форма не показывается

## 📚 Источники:

- OnchainKit типы: `node_modules/@coinbase/onchainkit/dist/minikit/hooks/useSwapToken.d.ts`
- OnchainKit тесты: `node_modules/@coinbase/onchainkit/src/minikit/hooks/useSwapToken.test.tsx`
- Farcaster типы: `node_modules/@coinbase/onchainkit/node_modules/@farcaster/miniapp-core/src/actions/SwapToken.ts`



# 📊 Проверка баланса контракта

## Информация о контракте:

- **Адрес:** `0xcd7092246c5DB86bC65C98fD943A18d409fCf03D`
- **Сеть:** Base (8453)
- **Explorer:** https://basescan.org/address/0xcd7092246c5DB86bC65C98fD943A18d409fCf03D

## Как проверить баланс:

### Способ 1: Через Basescan (самый простой)

1. Откройте: https://basescan.org/address/0xcd7092246c5DB86bC65C98fD943A18d409fCf03D
2. Перейдите на вкладку **"Contract"**
3. Нажмите **"Read Contract"**
4. Найдите функцию **"getContractBalance"**
5. Нажмите **"Query"** - это покажет баланс в wei
6. Разделите на 10^18 для получения количества MCT токенов

### Способ 2: Через консоль браузера на странице приложения

Откройте консоль (F12) на странице вашего приложения и выполните:

```javascript
// Если используется wagmi
const { createPublicClient, http, formatUnits } = await import('https://cdn.jsdelivr.net/npm/viem@latest/dist/esm/index.js');

const publicClient = createPublicClient({
  chain: {
    id: 8453,
    name: 'Base',
    network: 'base',
    nativeCurrency: { name: 'Ether', symbol: 'ETH', decimals: 18 },
    rpcUrls: {
      default: { http: ['https://mainnet.base.org'] }
    }
  },
  transport: http('https://mainnet.base.org')
});

const balance = await publicClient.readContract({
  address: '0xcd7092246c5DB86bC65C98fD943A18d409fCf03D',
  abi: [{
    inputs: [],
    name: 'getContractBalance',
    outputs: [{ internalType: 'uint256', name: '', type: 'uint256' }],
    stateMutability: 'view',
    type: 'function'
  }],
  functionName: 'getContractBalance'
});

console.log('Баланс контракта:', formatUnits(balance, 18), 'MCT');
```

### Способ 3: Через компонент ContractStats

В приложении уже есть компонент `ContractStats`, который показывает баланс. Проверьте, отображается ли он на странице.

### Способ 4: Проверка через API Basescan

```bash
# Получить информацию о токенах контракта
curl "https://api.basescan.org/api?module=account&action=tokenlist&address=0xcd7092246c5DB86bC65C98fD943A18d409fCf03D&apikey=YourApiKeyToken"
```

## Что проверить:

1. **Баланс MCT токенов** - должно быть достаточно для выплат
2. **Размер награды** - проверьте `dailyRewardAmount` (20,000 MCT)
3. **Сколько клеймов можно сделать** - баланс / размер награды

## Если баланс низкий:

1. Нужно пополнить контракт MCT токенами
2. Используйте функцию `fundContract` или отправьте токены напрямую на адрес контракта
3. Убедитесь, что отправляете именно MCT токены, а не ETH

## Адрес токена MCT:

Нужно найти адрес токена MCT на Base. Обычно он указан в конфигурации проекта или в контракте.






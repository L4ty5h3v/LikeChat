# 🧹 Инструкция по очистке кеша Vercel

## Проблема
Модальное окно "SYSTEM INITIALIZATION" все еще появляется из-за кеша Vercel или старого bundle.

## ⚠️ КРИТИЧЕСКИ ВАЖНО: Где был удален код модального окна

Код модального окна был удален из:
- `lib/memory-db.ts` - удалена проверка на первые 10 пользователей
- `lib/upstash-db.ts` - удалена проверка на первые 10 пользователей
- `pages/submit.tsx` - модальное окно никогда не рендерилось здесь (только удаление через скрипты)
- `pages/_app.tsx` - модальное окно никогда не рендерилось здесь (только удаление через скрипты)

**Если модальное окно все еще видно, это 100% кеш Vercel или старый bundle!**

## Решение 1: Очистка кеша через Vercel Dashboard (ПРИОРИТЕТ)

1. Откройте [Vercel Dashboard](https://vercel.com/dashboard)
2. Выберите ваш проект `likechat-farcaster`
3. Перейдите в **Settings** → **Git**
4. Найдите секцию **Build Cache**
5. Нажмите **"Clear Build Cache"** или **"Purge Build Cache"**
6. Подтвердите очистку

## Решение 2: Redeploy с игнорированием кеша (ОБЯЗАТЕЛЬНО!)

1. В Vercel Dashboard откройте ваш проект
2. Перейдите на вкладку **Deployments**
3. Найдите последний deployment
4. Нажмите на **три точки (⋯)** рядом с deployment
5. Выберите **"Redeploy"**
6. **⚠️ КРИТИЧЕСКИ ВАЖНО**: Найдите опцию **"Use existing Build Cache"** → **ОТКЛЮЧИТЕ ЕЕ** (чтобы игнорировать кеш)
   - Если опции нет, нажмите **"Redeploy"** и в появившемся окне отключите кеш
7. Нажмите **"Redeploy"**

## Решение 3: Очистка кеша через CLI

```bash
# Установите Vercel CLI (если еще не установлен)
npm i -g vercel

# Войдите в Vercel
vercel login

# Очистите кеш проекта
vercel env pull --force
```

## Решение 4: Принудительный новый build ID

Мы уже добавили `generateBuildId` в `next.config.js`, который генерирует уникальный ID при каждом build. Это должно заставить Vercel пересобрать все файлы.

## Решение 5: Очистка кеша браузера

1. Откройте DevTools (F12)
2. Перейдите в **Application** → **Storage**
3. Нажмите **"Clear site data"**
4. Или используйте **Ctrl+Shift+Delete** → выберите "Cached images and files"
5. Попробуйте в **Incognito режиме** (Ctrl+Shift+N)

## Решение 6: Проверка Network Tab

1. Откройте DevTools (F12) → **Network**
2. Обновите страницу (Ctrl+R)
3. Проверьте все `.js` файлы:
   - Если статус **304 (Not Modified)** → это кеш!
   - Если статус **200** → файл свежий
4. Если видите 304, добавьте `?v=timestamp` к URL или очистите кеш

## Что проверить после очистки

1. Откройте консоль (F12 → Console)
2. Должны появиться логи:
   - `🔍 [_DOCUMENT] Modal removal script loaded`
   - `🔴 [_DOCUMENT-IMMEDIATE] Found modal...` (если модальное окно найдено)
3. Если логов нет → загружается старый JavaScript из кеша

## Если проблема сохраняется

1. Проверьте, что код модального окна действительно удален из всех файлов
2. Проверьте, нет ли модального окна в:
   - `OnchainKitProvider` (может рендерить свои модальные окна)
   - `FarcasterAuthProvider`
   - Глобальных компонентах
3. Проверьте Service Workers (Application → Service Workers → Unregister)


# Полная последовательность работы приложения LikeChat Farcaster

## 🎯 Полный Flow от начала до конца

### 1️⃣ Главная страница (`/`) - Авторизация и выбор активности

**Шаги:**
1. Пользователь открывает приложение
2. Нажимает кнопку **"Connect Farcaster"** или **"Connect Wallet"**
3. Авторизуется через Farcaster кошелек
4. Данные пользователя сохраняются в `localStorage` как `farcaster_user`
5. Выбирает тип активности:
   - **❤️ LIKE NOW** - для лайков
   - **🔄 RECAST NOW** - для рекастов
   - **💬 COMMENT NOW** - для комментариев
6. Выбранная активность сохраняется в `localStorage` как `selected_activity`
7. Автоматический редирект на `/tasks` (через 500ms)

**Результат:** Пользователь переходит на страницу задач

---

### 2️⃣ Страница задач (`/tasks`) - Выполнение заданий

**Шаги:**
1. Загружаются все ссылки (задания) от других пользователей
2. Фильтрация по выбранному типу активности (`like`, `recast`, `comment`)
3. Пользователь видит список заданий (обычно 10 ссылок)
4. Для каждого задания:
   - Нажимает кнопку **"Open Post"** → ссылка открывается в новой вкладке
   - Выполняет действие (лайк/рекаст/комментарий) в Farcaster/Warpcast
   - Нажимает кнопку **"VERIFY COMPLETION"** для проверки выполнения
   - Система проверяет через Neynar API, выполнено ли действие
   - Если выполнено → задание помечается как завершенное (зеленый цвет ✓)
   - Если не выполнено → задание выделяется красным цветом с ошибкой ❌

**Автоматические проверки:**
- Каждые 2 секунды обновляется список заданий
- После открытия ссылки запускается автоматический polling (проверка каждые 30 секунд, до 10 попыток)

**Автоматические редиректы:**
- ✅ Если все 10 заданий выполнены → **редирект на `/buyToken`** (через 2 секунды)
- ✅ Если ссылка уже опубликована → остается на странице `/tasks`

**Результат:** После выполнения всех 10 заданий пользователь переходит на страницу покупки токена

---

### 3️⃣ Страница покупки токена (`/buyToken`) - Покупка MCT токена

**Шаги:**
1. Пользователь видит информацию о токене MCT
2. Подключает кошелек (Farcaster Wallet), если еще не подключен
3. Видит свой баланс USDC и MCT токенов
4. Нажимает кнопку **"❤️ BUY MRS. CRYPTO TOKEN FOR $0.10"**
5. Выполняется swap: **0.10 USDC → MCT токены** через Farcaster Swap API
6. Система отслеживает изменение баланса MCT токенов в реальном времени
7. После успешной покупки:
   - Токен помечается как купленный в базе данных (`token_purchased = true`)
   - Отправляется уведомление через MiniKit SDK
   - Публикуется cast в Warpcast с tx hash (если доступен)

**Автоматические редиректы:**
- ✅ После покупки токена → **автоматический редирект на `/submit`** (через 2 секунды)

**Кнопки после покупки:**
- Если токен куплен → показывается кнопка **"PUBLISH LINK →"** (ведет на `/submit`)
- Если токен не куплен → показывается кнопка покупки

**Важно:**
- Покупка токена совершается **только один раз**
- После покупки токена всегда можно опубликовать ссылку

**Результат:** После покупки токена пользователь автоматически переходит на страницу публикации ссылки

---

### 4️⃣ Страница публикации ссылки (`/submit`) - Публикация своей ссылки

**Шаги:**
1. Проверяется, что токен куплен (`token_purchased = true`)
2. Проверяется, что пользователь еще не публиковал ссылку
3. Пользователь вводит ссылку на свой cast (Farcaster/Warpcast URL)
4. Выбирает тип активности (должен совпадать с выбранным на главной странице)
5. Нажимает кнопку **"PUBLISH LINK"**
6. Система:
   - Валидирует URL (должен быть с `warpcast.com` или `farcaster.xyz`)
   - Извлекает cast hash из URL
   - Сохраняет ссылку в базу данных
   - Устанавливает флаг `link_published = true` в `sessionStorage` и `localStorage`

**Поздравление:**
- Показывается модальное окно с поздравлением:
  - 🎉 **"Поздравляем!"**
  - **"Ваше задание опубликовано!"**
  - **"Ваша ссылка теперь доступна в списке заданий для других пользователей."**
  - **"Следующие 10 пользователей пройдут вашу ссылку и выполнят выбранную активность."**

**После поздравления:**
- Пользователь нажимает кнопку **"Перейти к заданиям →"**
- Редирект на `/tasks?published=true`

**Результат:** Ссылка пользователя появляется в списке заданий для других пользователей

---

### 5️⃣ Возврат на страницу задач (`/tasks`) - Просмотр своей ссылки

**Шаги:**
1. Пользователь видит уведомление об успешной публикации (если `published=true` в URL)
2. Видит свою ссылку в списке заданий
3. Может остаться на странице и наблюдать, как другие пользователи выполняют его задание

**Результат:** Цикл завершен, пользователь может начать новый цикл с главной страницы

---

## 🔄 Упрощенный Flow (после покупки токена)

Если пользователь уже купил токен ранее:

1. **Страница покупки (`/buyToken`)** → Показывается кнопка **"PUBLISH LINK →"**
2. **Страница публикации (`/submit`)** → Публикация ссылки
3. **Поздравление** → Модальное окно
4. **Страница задач (`/tasks`)** → Просмотр своей ссылки

---

## 📋 Проверки на каждой странице

### Главная страница (`/`)
- ✅ Проверка авторизации через Farcaster
- ✅ Сохранение данных пользователя
- ✅ Сохранение выбранной активности

### Страница задач (`/tasks`)
- ✅ Проверка авторизации пользователя
- ✅ Проверка выбранного типа активности
- ✅ Проверка выполнения заданий через Neynar API
- ✅ Автоматическое обновление списка заданий каждые 2 секунды
- ✅ Автоматический polling после открытия ссылки (каждые 30 секунд)

### Страница покупки (`/buyToken`)
- ✅ Проверка авторизации пользователя
- ✅ Проверка подключения кошелька
- ✅ Проверка баланса USDC (минимум 0.10 USDC)
- ✅ Проверка статуса покупки токена в БД
- ✅ Отслеживание изменения баланса MCT токенов в реальном времени

### Страница публикации (`/submit`)
- ✅ Проверка авторизации пользователя
- ✅ Проверка покупки токена (`token_purchased = true`)
- ✅ Проверка, что пользователь еще не публиковал ссылку
- ✅ Валидация формата URL (warpcast.com или farcaster.xyz)

---

## 🎯 Ключевые моменты

1. **Покупка токена совершается только один раз** - после покупки всегда можно публиковать ссылку
2. **После покупки токена можно публиковать ссылку сразу** - не требуется выполнение всех задач
3. **Задания проверяются автоматически** через Neynar API
4. **Ссылка пользователя появляется в списке заданий** для других пользователей
5. **Защита от повторной публикации** через флаг `link_published`
6. **Автоматические редиректы** между страницами для удобства пользователя

---

## 🔐 Состояния и флаги

### `selected_activity`
- Хранится в `localStorage`
- Типы: `'like'`, `'recast'`, `'comment'`
- Используется для фильтрации заданий

### `link_published`
- Хранится в `sessionStorage` и `localStorage`
- Значение: `'true'` или отсутствует
- Блокирует повторный доступ к странице `/submit`
- Очищается на главной странице при новом цикле

### `token_purchased`
- Хранится в базе данных (Upstash Redis)
- Значение: `true` или `false`
- Определяет, может ли пользователь публиковать ссылку

### `completed_links`
- Хранится в базе данных
- Массив ID завершенных ссылок
- Используется для подсчета выполненных заданий

---

## 🚀 Последовательность редиректов

```
Главная (/) 
  ↓ (выбор активности)
/tasks 
  ↓ (выполнение 10 заданий)
/buyToken 
  ↓ (покупка токена, автоматически через 2 сек)
/submit 
  ↓ (публикация ссылки)
Поздравление (модальное окно)
  ↓ (нажатие кнопки)
/tasks?published=true
```

---

## ✅ Итоговая последовательность

1. **Главная** → Авторизация → Выбор активности → `/tasks`
2. **`/tasks`** → Выполнение 10 заданий → `/buyToken`
3. **`/buyToken`** → Покупка токена → `/submit` (автоматически)
4. **`/submit`** → Публикация ссылки → Поздравление → `/tasks`
5. **`/tasks`** → Просмотр своей ссылки в списке заданий

**Цикл завершен!** 🎉


# 📋 Объяснение Farcaster Exec транзакции

## Что вы показали:

Это **multicall транзакция** от Farcaster кошелька с функцией `exec(address,address,uint256,address,bytes)`.

---

## 🔍 Структура транзакции:

```json
{
  "atomicRequired": false,
  "calls": [
    {
      "data": "0x...", // Данные для выполнения
      "to": "0x0000000000001ff3684f28c67538d4d072c22734" // Роутер/Агрегатор
    }
  ],
  "chainId": "0x2105", // Base (8453)
  "from": "0x12029632C95dE52D403e18A03D29648b877b2889",
  "version": "2.0.0"
}
```

---

## 📊 Что происходит в транзакции:

1. **Multicall роутер** (`0x0000000000001ff3684f28c67538d4d072c22734`)
   - Это адрес роутера, который выполняет несколько операций в одной транзакции

2. **Внутри данных (data):**
   - Вызовы `approve` для USDC
   - Вызовы `transfer` для токенов
   - Swap операции через DEX

3. **Токены:**
   - USDC: `0x833589fCD6eDb6E08f4c7C32D4f71b54bdA02913`
   - Различные адреса получателей

---

## 🚀 Как использовать в коде:

Farcaster SDK может иметь метод `exec` для выполнения таких транзакций:

```typescript
const result = await sdk.actions.exec({
  target: routerAddress,
  callData: encodedData,
  value: '0',
});
```

---

## ✅ Что я сделал:

1. **Создал `lib/farcaster-swap-exec.ts`**
   - Функция для использования `sdk.actions.exec`
   - Поддержка прямого выполнения транзакции

2. **Обновил `lib/farcaster-swap.ts`**
   - Добавлена попытка использовать `exec` перед `openUrl`
   - Fallback на открытие URL если exec не работает

---

## 🔧 Как это работает:

1. **Попытка exec:**
   - Пробуем выполнить транзакцию напрямую через `sdk.actions.exec`
   - Если доступно - выполняем multicall транзакцию

2. **Fallback на openUrl:**
   - Если exec не работает - открываем swap интерфейс
   - Пользователь подтверждает транзакцию в кошельке

---

## 📝 Примечание:

Точный формат `exec` может отличаться в зависимости от версии Farcaster SDK. 
Если у вас есть документация по формату exec - можно уточнить реализацию.

---

## ✅ Готово!

Теперь покупка токена пытается использовать `exec` для прямого выполнения транзакции через Farcaster кошелек!


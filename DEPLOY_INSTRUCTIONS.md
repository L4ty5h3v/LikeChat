# 🚀 Инструкция по развертыванию контракта

## ⚠️ ВАЖНО: Перед развертыванием

Для развертывания контракта вам нужен **приватный ключ** кошелька с ETH на Base.

**⚠️ БЕЗОПАСНОСТЬ:**
- Никогда не коммитьте приватный ключ в Git!
- Используйте отдельный кошелек для развертывания
- Храните приватный ключ в безопасности

---

## 📋 Шаг 1: Настройка .env.local

Добавьте в `.env.local`:

```env
# Приватный ключ кошелька для развертывания (БЕЗ 0x в начале)
PRIVATE_KEY=your-private-key-here

# Адрес токена (уже настроен)
NEXT_PUBLIC_TOKEN_CONTRACT_ADDRESS=0x454b4180bc715ba6a8568a16f1f9a4b114a329a6

# RPC URL для Base (опционально)
NEXT_PUBLIC_BASE_RPC_URL=https://mainnet.base.org
```

---

## 📋 Шаг 2: Компиляция контрактов

```bash
npm run compile
```

Это скомпилирует контракты и проверит, что все правильно.

---

## 📋 Шаг 3: Развертывание

### Вариант A: Покупка через ETH (Base Mainnet)

```bash
npm run deploy:eth
```

### Вариант B: Покупка через USDC (Base Mainnet)

```bash
npm run deploy:usdc
```

### Для тестовой сети Base Sepolia:

```bash
# ETH
npm run deploy:eth:sepolia

# USDC
npm run deploy:usdc:sepolia
```

---

## 📋 Шаг 4: После развертывания

После успешного развертывания скрипт покажет:

1. **Адрес контракта** - скопируйте его
2. **Инструкции** по добавлению в `.env.local`

Добавьте в `.env.local`:

**Для ETH:**
```env
NEXT_PUBLIC_TOKEN_SALE_CONTRACT_ADDRESS=0xВашАдресКонтракта
NEXT_PUBLIC_USE_USDC_FOR_PURCHASE=false
```

**Для USDC:**
```env
NEXT_PUBLIC_TOKEN_SALE_USDC_CONTRACT_ADDRESS=0xВашАдресКонтракта
NEXT_PUBLIC_USE_USDC_FOR_PURCHASE=true
NEXT_PUBLIC_USDC_CONTRACT_ADDRESS=0x833589fCD6eDb6E08f4c7C32D4f71b54bdA02913
```

---

## 📋 Шаг 5: Пополнение контракта токенами

После развертывания нужно перевести токены на контракт продажи:

1. Откройте ваш кошелек (MetaMask и т.д.)
2. Перейдите на Base
3. Найдите токен Mrs Crypto Token
4. Переведите токены на адрес контракта продажи
5. Минимальное количество для тестирования: 10-100 MCT

**Проверка баланса контракта:**
- Откройте контракт на BaseScan
- Вызовите функцию `availableTokens()`
- Должно быть > 0

---

## 🔍 Проверка развертывания

1. **Проверьте контракт на BaseScan:**
   ```
   https://basescan.org/address/YOUR_CONTRACT_ADDRESS
   ```

2. **Проверьте цену:**
   - Вызовите `pricePerToken()` на контракте
   - Для ETH: должно быть в wei (например, 1000000000000000 = 0.001 ETH)
   - Для USDC: должно быть в 6 decimals (например, 2500000 = 2.5 USDC)

3. **Проверьте стоимость 0.10 MCT:**
   - Вызовите `costFor(100000000000000000)` (0.10 MCT в wei)
   - Для ETH: должно быть примерно 0.0001 ETH
   - Для USDC: должно быть примерно 0.25 USDC

---

## 🆘 Решение проблем

### Ошибка: "PRIVATE_KEY не установлен"
- ✅ Решение: Добавьте `PRIVATE_KEY=your-key` в `.env.local`

### Ошибка: "insufficient funds for gas"
- ✅ Решение: Пополните кошелек ETH на Base

### Ошибка компиляции
- ✅ Решение: Убедитесь, что установлены все зависимости: `npm install`

### Контракт развернут, но нет токенов
- ✅ Решение: Переведите токены на адрес контракта продажи

---

## 📝 Примеры команд

```bash
# Компиляция
npm run compile

# Развертывание на Base Mainnet (ETH)
npm run deploy:eth

# Развертывание на Base Sepolia (тестовая сеть)
npm run deploy:eth:sepolia

# Проверка конфигурации после развертывания
npm run check:config
```

---

## ✅ Чек-лист после развертывания

- [ ] Контракт развернут
- [ ] Адрес контракта добавлен в `.env.local`
- [ ] Контракт проверен на BaseScan
- [ ] Токены переведены на контракт
- [ ] Цена проверена
- [ ] `npm run check:config` показывает успех
- [ ] Готово к тестированию!

---

**Готово!** Теперь можно тестировать покупку токена в приложении. 🚀

